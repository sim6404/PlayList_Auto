{
  "version": 2,
  "builds": [
    {
      "src": "dashboard/app.py",
      "use": "@vercel/python",
      "config": {
        "includeFiles": ["src/**", "config/channel_dna.json"]
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "dashboard/app.py"
    }
  ],
  "env": {
    "DATA_DIR": "/tmp/data",
    "LOG_DIR": "/tmp/logs",
    "VERCEL": "1"
  },
  "regions": ["icn1"]
}
