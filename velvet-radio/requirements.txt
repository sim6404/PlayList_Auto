# ============================================================
#  Velvet Radio — Dashboard (Vercel 경량 의존성)
#  파이프라인 전체 의존성: requirements-full.txt 참조
# ============================================================

# Web
Flask>=3.0
Werkzeug>=3.0

# Data models & config
pydantic>=2.0
pydantic-settings>=2.0
python-dotenv>=1.0

# Claude API (SEO 생성용)
anthropic>=0.40

# HTTP
httpx>=0.27
tenacity>=8.0

# Logging
structlog>=24.0
